# Proceed from the roadmap: how I would read, then what I would ship

Status of this plan: reading + ranking only. No code in this pass.

The previous turn mapped the package. This plan answers a narrower question: given `specs/ROADMAP.md`, `CHANGELOG.md`, and HEAD, **how do I actually read the code, in what order, and what is the next implementable work?**

---

## What the documents actually say

Three layers, not one program:

| Document                                       | Job                   | Honest status                                                                                                                                                |
| ---------------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `specs/ROADMAP.md` + `specs/effect-tui/01..20` | Architecture program  | **Proposed.** Every spec has a landed _slice_. No spec has passed its release gate.                                                                          |
| `specs/PRODUCT_ROADMAP.md`                     | User-outcome horizons | **Proposed.** Historical C1–C3 / H8 IDs. Product measurement windows still open.                                                                             |
| `CHANGELOG.md`                                 | What shipped to users | After v1.347 the EOT mentions stop. v1.348–1.355 is mobile pairing, LAN, camera, CI format. HEAD is a finished Database Effect migration (v1.356 on origin). |

The roadmap’s own rule: _a landed slice is the contract (a table + a test that fails when it changes), not the migration that contract exists to govern._

What that means in practice: EOT-02 typed `runService`; it did not migrate `prompt.ts`. EOT-11 covered `suppressEmptyTextResult`; it did not converge AI SDK → `LLMEvent`. EOT-01 can reproduce the never-paints hang; it has not ratified a budget.

HEAD vs the catalog:

- Branch `live-main`, **behind origin by 1** (`98ac58cd87 release: v1.356.0`).
- Working tree has **uncommitted auth/account/router edits** (`httpapi/account.ts`, `auth.ts`, `users.ts`, `identity-auth.ts`, `server-router.ts`) plus an untracked `test/server/local-account-session.test.ts`. That is EOT-12-adjacent work in flight — not on the EOT landed-slices table.
- Last ~12 commits on HEAD are `refactor(database): move * onto Effect`. `specs/storage/retire-database-wrapper.md` now says **Groups 1–4 complete** (2026-09-15): `Database.syncDb()` has zero callers in `src`. That is EOT-09-adjacent, outside the EOT IDs.

Do not start a new EOT slice on top of dirty auth files and a one-commit lag. Stabilize the tree first.

---

## Constraints I will not violate

From `AGENTS.md` / `specs/ROADMAP.md` non-negotiables:

1. HttpApi remains the contract. No Hono, no handwritten SDK, no `Schema.Unknown` on domain success.
2. TUI must not import `packages/nikcli` internals.
3. Durable state stays SQL repos. Do not reintroduce JSON `Storage`.
4. Do not add the ~350-file nikcli suite back to CI. Local whole-suite is `bun run test:ci`.
5. One slice per PR. Do not combine Effect upgrade + transport + virtualization + observability + CLI in one change.
6. `effect-sqlite-package.md` is **blocked** (deferred-choices clause + decision 3). The wrapper retirement did not need it and is already done.
7. Ask before git operations.

---

## How I would read (Process + Effect first)

The architecture map is already in session. I would **not** re-survey `packages/*`. I would walk the live process until I can name, for each stop: owner, cancellation, failure type, and the test that would catch a regression.

Each stop: read the file, then the matching `test/` file, then the spec paragraph that claims a slice landed. If the test does not pin the claim, the slice is documentation.

### Day 0 — stabilize, then the process

1. `git status` / `git log HEAD..origin/live-main`. Decide: stash or finish the uncommitted auth work before any EOT slice. Do not mix.
2. Read, in this order, without editing:

| Order | File                                                                            | What I am checking                                                                          |
| ----- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 1     | `packages/nikcli/src/index.ts`                                                  | Process entry. Lazy import of the CLI.                                                      |
| 2     | `packages/nikcli/src/cli/main-effect.ts`                                        | Bootstrap _before_ parse (Log, IslandBridge, SIGHUP). `BunRuntime.runMain`.                 |
| 3     | `packages/nikcli/src/cli/framework/{spec,runtime}.ts`                           | Command tree as data. `cmd({ bootstrap, teardown })`. EOT-18 parser is done; policy is not. |
| 4     | `packages/nikcli/src/cli/bootstrap.ts`                                          | Dispose **outside** the body (R3). The interrupt-self bug.                                  |
| 5     | `packages/nikcli/src/effect/runtime.ts`                                         | `sharedMemoMap`, `runPromiseWithLayer` `R extends ROut`, LogRedirect.                       |
| 6     | `packages/nikcli/src/effect/run-service.ts`                                     | Shared `R` between layer and effect.                                                        |
| 7     | `packages/nikcli/src/effect/instance-scope.ts`                                  | Fiber Exit replay, caller interrupt → inner fiber. Workspace still a label (EOT-16).        |
| 8     | `packages/nikcli/src/effect/instance-ref.ts` + `with-instance.ts`               | ALS vs `InstanceRef`. `withCurrentInstance` (~165 Promise call sites).                      |
| 9     | `packages/nikcli/src/project/instance.ts`                                       | `ScopedCache`, infinite TTL, per-directory `ManagedRuntime`.                                |
| 10    | `packages/nikcli/test/effect/{runtime,instance-scope,instance-ambient}.test.ts` | Whether EOT-02’s typing slice is actually gated.                                            |

Exit of Day 0: I can draw one request from argv to `Instance.provide` and say where Effect starts and Promise takes over.

### Day 1 — HTTP as the Effect boundary that already exists

This is the highest-leverage Effect surface. Handlers already validate responses; a wrong schema is a failed request, not a dropped field.

| Order | File                                                           | What I am checking                                                               |
| ----- | -------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| 1     | `src/server/server.ts`                                         | `Bun.serve`, in-process `fetch()`, port 4096→ephemeral.                          |
| 2     | `src/server/server-router.ts`                                  | Auth, directory, session→instance. “not found” vs “store damaged”.               |
| 3     | `src/server/httpapi/public.ts`                                 | `PublicHttpApi.Api` (served) vs `PublicApi` (codegen). Auth middleware wrapping. |
| 4     | `src/server/httpapi/bridge.ts`                                 | Effect HttpApi ↔ `Request`/`Response`.                                           |
| 5     | `src/server/httpapi/security.ts` + `auth.ts`                   | Where the uncommitted local-account work sits.                                   |
| 6     | `src/server/httpapi/event-feed.ts`                             | Encode-once, 4096-frame lag. Byte bound still missing (EOT-04).                  |
| 7     | `test/server/` matching files; `bun run check:routes` mentally | Open-payload inventory (EOT-10 slice).                                           |

Exit of Day 1: I know which failures can still look like success (EOT-10 remaining), and whether the dirty auth files are a slice or a distraction.

### Day 2 — session loop, only at the crossings

I would **not** read `prompt.ts` (75 KB) cover to cover. I would read the Promise↔Effect crossings and the processor’s cancellation:

| Order | File                                                               | What I am checking                                                                                         |
| ----- | ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| 1     | `session/index.ts`                                                 | `Session.Service`, busy gate, `runPromiseWithLayer(Config.defaultLayer, …)`.                               |
| 2     | `session/processor.ts`                                             | Turn loop, abort, `askPermission`, doom-loop, coalesced `Bus.publish` (the per-token `flatMap` exception). |
| 3     | `session/llm.ts` + `session/llm/llm-event-adapter.ts`              | AI SDK still owns production. Adapter is a seam, not the path (EOT-11).                                    |
| 4     | `permission/next.ts` + `ruleset.ts`                                | Last-rule-wins, audit of the matching rule (EOT-17 slice).                                                 |
| 5     | `tool/registry.ts`                                                 | Gating. Not every tool file.                                                                               |
| 6     | `bus/index.ts`                                                     | Best-effort Promise publish (B13). Changing it is a compatibility break.                                   |
| 7     | `database/database.ts` + `test/database/wrapper-inventory.test.ts` | Confirm `syncDb` ratchet at zero. This work is **done**; do not reopen.                                    |

Exit of Day 2: a list of nested Promise→Effect→Promise round-trips inside service bodies (EOT-02 remaining), ranked by call frequency.

### Day 3 — TUI host only, then stop

TUI internals (virtualization, input) are P3. I would read only the host seam, because EOT-01’s hang lives here:

| Order | File                                    | What I am checking                                                          |
| ----- | --------------------------------------- | --------------------------------------------------------------------------- |
| 1     | `cli/handlers/default.ts`               | Main thread: worker spawn, RPC fetch, event source.                         |
| 2     | `cli/cmd/tui/worker.ts`                 | `Server.fetch` in-process, GlobalBus → RPC, browser-control daemon handoff. |
| 3     | `packages/tui/src/host/standalone.ts`   | Second consumer. Config failure must not become `{}` (EOT-10).              |
| 4     | `packages/nikcli/script/tui-startup.ts` | Probe: firstPaint vs usablePrompt, `repro:startup-hang`.                    |
| 5     | `test/tui/streaming-churn.test.tsx`     | Non-vacuous destroy assertions (B17).                                       |

I would **not** enter `packages/tui/src/routes/session/` or `component/prompt/` until EOT-01 can paint reliably. Rendering work on a hang is noise.

---

## What I would **not** read yet

- `session/prompt.ts` as a rewrite target. Roadmap: extract by responsibility after evidence, not by file size.
- Plugin v2 runtime migration (EOT-14). Manifest/capability/budget slices landed; runtime was audited and left.
- Workspace-as-scope (EOT-16). Depends on EOT-02 teardown tests that do not exist yet.
- Mobile/companion bridge (EOT-19). P3, depends on 04/08/12/15.
- `effect-sqlite-package.md`. Blocked. Wrapper retirement already finished without it.
- Full `packages/identity` Worker. EOT-12 forbids changing it without a separate contract review.
- Product-roadmap C3 Railway two-upload window. Needs production deploys, not a code-reading pass.

---

## Ranked next work (after the reading)

Priority is: **dirty tree → EOT-01 hang → EOT-02/10 characterization that the reading proved is still open → one vertical slice.**

### Tier 1 — do first

**T1. Stabilize the tree (hours, not a spec)**

- Inspect the uncommitted auth/account/router diff. Either finish it as its own PR (local account session + `local-account-session.test.ts`) or stash it.
- Fast-forward/rebase onto `origin/live-main` (`v1.356.0`) after asking.
- Do not start an EOT slice on a dirty worktree.

**T1. EOT-01 hang — the actual P0 gate**

Roadmap first implementable slice #1, still blocked:

> Compiled binary intermittently never paints (1 in 3–8). Last output is OpenTUI capability queries (OSC 10/11, XTGETTCAP, Kitty graphics, …). Main thread parked in `kevent64`. `--print-logs` is a false fix (crosses the probe’s paint threshold). Forcing `OPENTUI_*` still hangs.

Reading path for the hang only:

1. `script/tui-startup.ts` + any `repro:startup-hang` script.
2. `@opentui/core` renderer creation (capability negotiation). Installed decls, not a fork.
3. `packages/tui/src/app.tsx` renderer construction (~line 102, 45 FPS cap).
4. Confirm whether 0.5.11 still waits unbounded for OSC replies.

Acceptance: hang is either fixed or classified unsupported with a non-zero check (EOT-01 failure clause). Then collect 30 warm / 10 cold on a **compiled** binary, store raw samples, ratify candidate budgets. No production behavior change in the measurement PR.

Until this paints, **no TUI performance slice, no EOT-06/07/08 continuation that claims speed.**

**T1. EOT-02 — one caller chain, no nested round-trip**

Dependencies: EOT-01 (measurement; hang-fix can proceed in parallel with _characterization_, not with “make it faster”).

After Day 0–2 reading, pick **one** bounded service already using `defaultLayer` — likely `Session` get/create or `PermissionNext.ask` — and:

- Characterize the Promise facade.
- Remove inner `runPromiseWithLayer` → `await` → wrap-again inside the service body.
- Extend `test/effect/instance-scope.test.ts`: two instances, finalizer on success/failure/defect/interrupt/cancel-during-acquire.
- One root `bun run typecheck` at the end, not per file.

Do **not** migrate `prompt.ts`. Do **not** delete `withCurrentInstance` until an inventory shows the 165 sites are gone.

**T1. EOT-10 — only if the dirty auth files are this**

If the uncommitted work is “local account session must not look like success/empty”, finish it as the EOT-10/12 pilot (account + TUI-config already had a slice: config failure no longer becomes `{}`). Add the new test, regenerate clients if the contract moved, `bun run check:routes`.

If it is unrelated drive-by typing, revert or isolate.

### Tier 2 — after T1 evidence

| ID     | Why next                        | What I would actually do                                                                                                                                        |
| ------ | ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EOT-03 | Dialog lifecycle; hang-adjacent | Carry abort + generation through **one** complete dialog (login is the recurring surface). `attempt.adopt` already landed.                                      |
| EOT-20 | Harness for everything else     | Already has disjoint layers + `preserveTestEnv`. Next: one more flaky integration → barrier. Cheap, unblocks later specs.                                       |
| EOT-13 | Session group spans             | Schema + redaction landed. Instrument `session` end-to-end, verify redaction, keep live panel rate-limited. Hold to EOT-01 overhead budget (5%).                |
| EOT-04 | After 02+10                     | Delivery-class registry exists. Classify event types and test overload **before** admission caps. Keep encode-once.                                             |
| EOT-12 | If T1 auth is clean             | Legal-transition table landed. Pilot **token refresh** through existing TUI flow. No PKCE downgrade, no skipped account creation. Controlled local issuer only. |

### Tier 3 — do not start

EOT-11 adapter convergence, EOT-14 plugin runtime, EOT-15 snapshot barrier, EOT-16 workspace scope, EOT-06 measured virtualization, EOT-07 controller extraction, EOT-19 mobile bridge, EOT-18 remaining policy (exit codes / headless). All L/High multi-PR migrations. Their contracts exist; the migrations wait on T1 gates.

Storage: wrapper retirement is **complete**. Next storage item is not “more Effect on repos”. Leave `effect-sqlite-package.md` blocked.

---

## Verification I would run (when implementing, not now)

From `packages/nikcli`, via `monitor`. Never root `bun test`.

| After                      | Command                                                                                                                           |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| Effect/instance            | `bun test test/effect/runtime.test.ts test/effect/instance-scope.test.ts test/effect/instance-ambient.test.ts`                    |
| HTTP/auth                  | `bun test test/server/` (narrow file if possible), `bun run check:routes`, `bun run generate:httpapi-clients` if contract changed |
| Database (regression only) | `bun test test/database/wrapper-inventory.test.ts test/database/transaction-semantics.test.ts`                                    |
| Hang / startup             | `bun run bench:startup <compiled-bin>`; `repro:startup-hang` if present                                                           |
| TUI host                   | `bun run smoke:tui`; `packages/tui` `bun run smoke:standalone`                                                                    |
| Any implementation         | one root `bun run typecheck` at the end                                                                                           |
| Release-sized              | local `bun run test:ci`                                                                                                           |

Quote pass/fail counts and exit codes. Do not paraphrase “tests pass”.

---

## First hour on the next implementation session

1. Ask: commit/stash the auth diff, rebase onto `v1.356.0`?
2. Read the ten Day-0 files + three `test/effect` files.
3. Decide T1 target: hang vs one `runService` chain vs finish local-account test.
4. Stop. Do not open `prompt.ts`.
